#!/usr/bin/env bash
# 完整安装包流程仅支持 Windows（Inno Setup + PyInstaller）。请使用：
#   pnpm run build:release
# 或在仓库根执行: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-release.ps1
set -euo pipefail
echo "Use: pnpm run build:release   (see 开发文档.md)" >&2
exit 1
